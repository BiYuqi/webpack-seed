<!DOCTYPE html>
<html lang="zh-cn">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0,maximum-scale=1.0, user-scalable=no">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <meta name="description" content="基于webpack4, babel7 开发可复用的现代化网站 解决非SPA应用, html复用, 模块化开发等问题">
  <title><% if (pageTitle) { %> <%= pageTitle %> <% }  %></title>
  <!--[if lt IE 9]>
  <% for(var i in fixIePart){ %>
    <script type="text/javascript" src="<%= fixIePart[i] %>"></script>
  <% } %>
  <![endif]-->
  <!-- 静态样式文件 -->
  <% for(var i in cssPart){ %>
  <link rel="stylesheet" href="<%= cssPart[i] %>" crossorigin="anonymous">
  <% } %>
  <!-- 第三方统计 -->
  <% if(isProduction) { %> <%= googleAnalyzer() %> <% } %>
</head>
<body>
