import content from './{{fileName}}.ejs'
import layout from 'layout/{{templateDir}}/{{templateName}}.js'
const pageTitle = '{{pageTitle}}'

const temp = layout.init({ pageTitle }).run(content())

export default temp
